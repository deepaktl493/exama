name: Access Secret Example

on: [push]

jobs:
  use-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Use API Key
        run: echo "API key is $API_KEY"
        env:
          API_KEY: ${{ secrets.API_KEY }}
